{
  "name": "bob-resources",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cp": "cp -R ./src/static ./dist",
    "start": "npm run cp && tsc --noEmit && parcel src/index.html --log-level 4",
    "build": "parcel build src/index.html"
  },
  "author": "jethro larson",
  "license": "UNLICENSED",
  "devDependencies": {
    "parcel": "^1.12.4",
    "typescript": "^3.9.2"
  },
  "dependencies": {
    "csx": "^10.0.1",
    "preact": "^10.4.1",
    "redux-zero": "^5.1.6",
    "typestyle": "2.0.4"
  }
}
